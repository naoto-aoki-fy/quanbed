Bootstrap: docker
From: nvidia/cuda:12.6.2-devel-rockylinux9

%post
    yum check-update || echo "[info] Update exists" 1>&2
    # yum install -y epel-release
    yum install -y numactl numactl-devel numactl-libs
    # yum update -y
    yum clean all

%runscript
    exec /bin/bash --login -i
    